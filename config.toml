langs.en = 1.0
langs.cs = 0.0

finger_strengths = [0.8, 1.0, 0.7, 0.4]  # index to pinkie

# stats
sfb = 0.25  # same fimger bigrams
inroll = 1.25  # inward rolls
outroll = 0.75  # outward rolls

alter = 1.25  # hand alteration, below 1.0 for more alteration

home_row = 1.0  # high value enforces the most frequent chars on home row
prefer_top_row = 0.0  # negative for more prevalent bottom row
row_change = 0.5
independence = 1.0  # makes bigrams by neighbor fingers more significant
mono_vs_bi = [0.5, 0.5]

# misc
enforce_balance = 1.0
vowel_side = true  # false for left, true for right
keep_familiar = 0.5  # 0 has no effect, high value for fewer changes
ref_layout = """
q w f p   l u y -
a r s t   n e i o
z x c d   h - - -
"""