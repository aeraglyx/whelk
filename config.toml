langs.en = 1.0
langs.cs = 0.0

finger_strengths = [0.8, 1.0, 0.7, 0.4]  # index to pinkie

# stats
sfb = 0.2  # same finger bigrams
inroll = 1.3  # inward rolls
outroll = 0.9  # outward rolls
alter = 1.4  # hand alteration, above 1.0 for more alteration

home_row = 1.2  # high value enforces the most frequent chars on home row
prefer_top_row = 0.2  # negative for more prevalent bottom row
row_change = 0.8
independence = 1.0  # makes close-by bigrams more significant

# misc
enforce_balance = 1.8
vowel_side = true  # false for left, true for right
keep_familiar = 0.0  # 0 has no effect, high value for fewer changes
ref_layout = """
q w f p   l u y -
a r s t   n e i o
z x c d   h - - -
"""

iterations = 128
initial_states = 16
bigram_quality = 0.99  # 1.0 will be the most accurate at the cost of speed